# 审查归档摘要

归档时间：2026-03-07
状态：已收口

## 本轮已完成事项
- 文档已对齐到当前真实结构：`app/commander.py`、`app/train.py`、`app/web_server.py`、`invest/*` 分包布局。
- 打包配置已修正：`pyproject.toml` 改为自动发现子包，避免漏打 `brain.*`、`market_data.*`、`invest.*`。
- 训练链路的主要副产物已支持路径注入：训练输出、会议记录、配置审计日志、配置快照。
- Web 层已补齐运行路径配置面板与 `/api/runtime_paths` 接口，做到 `Web / Commander / Train` 三入口一致。
- 先前审查提出的 `brain/runtime.py` tool 参数静默吞错问题已修复，不再把非法 JSON 默默降级为 `{}`。

## 当前判断
- 仓库当前处于“结构已稳定、治理已收口、可继续做业务优化”的状态。
- 当前主要矛盾已不再是兼容壳或路径漂移，而是后续是否继续做更深层的模块拆分与职责收紧。
- 现阶段没有阻断运行、测试或安装的治理类遗留问题。

## 剩余建议（非阻断）
- 继续拆分 `app/train.py`，把流程编排、评估汇总、优化触发拆成更小的服务对象。
- 继续收紧 `app/commander.py`，把 CLI、运行态、状态持久化进一步解耦。
- 对 Web 配置接口补更多契约测试，覆盖错误路径、相对路径与运行时热更新场景。
- 将根目录临时分析文件逐步迁入正式文档体系，减少仓库顶层“过程性文件”。

## 归档指向
- 全面审计文档：`docs/项目审计0307.md`
- 去兼容化结果：`docs/COMPAT_CLEANUP_REPORT.md`
