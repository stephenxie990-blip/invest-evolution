# Benchmark Fixture Set

Provenance:
- `leaderboard.sample.json` is extracted from a tracked historical output
  snapshot.
- `release_gate_divergence_report.sample.json` is a minimized canonical
  release-gate report fixture retained only for parser and shape validation.

Purpose:
- Provide a tiny representative dataset for parser/shape validation.
- Replace previously tracked bulk output directories.
